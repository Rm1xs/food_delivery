import 'package:flutter/material.dart';

class FoodMainFilters extends StatelessWidget {
  const FoodMainFilters({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
